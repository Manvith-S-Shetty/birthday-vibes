import React from "react";
import { CreatorShell } from "@/components/creator/CreatorShell";

export const metadata = {
  title: "Create Birthday Gift — Wishlight Creator Studio",
  description: "Craft an elegant, personalized digital birthday experience for someone special.",
};

export default function CreatePage() {
  return <CreatorShell />;
}
